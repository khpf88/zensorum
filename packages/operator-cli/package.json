{
  "name": "@zensorum/operator-cli",
  "version": "0.1.0",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "bin": {
    "zensorum": "index.ts"
  },
  "dependencies": {
    "@zensorum/application-contracts": "workspace:*",
    "@zensorum/core": "workspace:*",
    "@zensorum/replay": "workspace:*",
    "@zensorum/validation": "workspace:*",
    "@zensorum/ledger": "workspace:*",
    "commander": "^13.0.0"
  },
  "scripts": {
    "build": "echo 'Building operator-cli...'",
    "clean": "rm -rf dist"
  }
}
